{
  "name": "@partneriq-io/node",
  "version": "1.0.1",
  "description": "Official Node.js SDK for PartnerIQ B2B SaaS Partner & Affiliate Engine",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": ["dist", "README.md", "CHANGELOG.md"],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --external:node:crypto --outfile=dist/index.js --format=esm --sourcemap && esbuild src/index.ts --bundle --platform=node --external:node:crypto --outfile=dist/index.cjs --format=cjs --sourcemap && tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run typecheck",
    "prepublishOnly": "npm run build && npm pack --dry-run"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "partneriq",
    "affiliate",
    "referral",
    "saas",
    "node",
    "sdk"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.14.0",
    "esbuild": "^0.25.0",
    "typescript": "~5.8.2"
  }
}
